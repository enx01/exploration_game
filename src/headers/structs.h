#ifndef HEADERS_H
#include "../lib/include.h"

// Elements

  // Button
  struct Button;

  typedef struct Button Button;

  // Label
  struct Label;

  typedef struct Label Label;

// Game_scenes

  // Menu
  struct Menu;

  typedef struct Menu Menu;

  // Settings
  struct Settings;

  typedef struct Settings Settings;

  // Game
  struct Game;

  typedef struct Game Game;

    // Game Elements
  
      // Player
      struct Player;

      typedef struct Player Player;

      // Crosshair
      struct Crosshair;

      typedef struct Crosshair Crosshair;

      //Health Bar
      struct Health_Bar;

      typedef struct Health_Bar Health_Bar;

      //Inventory
      struct Inventory;

      typedef struct Inventory Inventory;

        //Inventory Item Slot
        struct Inv_Item_Slot;

        typedef struct Inv_Item_Slot Inv_Item_Slot;

      //Item
      struct Item;

      typedef struct Item Item;

      //Pop up Menu
      struct Pop_Up;

      typedef struct Pop_Up Pop_Up;


#endif // !HEADERS_H
